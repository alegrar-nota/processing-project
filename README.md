# processing-project

# Función size

```java
// size tamaño de la ventana
void setup(){
  //size(width,height);
  //size(100,100); por defecto
  //size(640,480); // 1ª línea de códgio dentro del setup, sólo 1 vez
  //size(640,480,P2D); // por defecto, sin pasar como parámetro sería igual. renderizador 2D
  //size(640,480,P3D); // se modifica el motor a 3D
  //size(640,480,FX2D); // renderizador de alta calidad
  //size(640,480,PDF); // guardar en PDF. Hay que importar desde la biblioteca PDF para poder crear
  fullScreen();
}
```

